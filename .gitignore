# perf local store — local-first, lab-only; never commit measurement history
*.db
*.db-journal
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# Virtual environments
.venv/
venv/

# CodeGraph index — per-checkout, never commit
.codegraph/

# Local AI runtime state — the skill registry indexes THIS machine's skill
# directories with absolute paths, so it is per-checkout by nature.
# Regenerate with: gentle-ai skill-registry refresh
.atl/

# Editors / OS
.DS_Store
